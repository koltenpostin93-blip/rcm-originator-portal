# Leave unset to use a local SQLite file (rcm_portal.db).
# Set to a Postgres URL when this moves to the client's own server, e.g.:
# DATABASE_URL=postgresql://user:password@host:5432/rcm_portal
DATABASE_URL=
